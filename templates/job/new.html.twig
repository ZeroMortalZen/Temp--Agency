{% extends 'base.html.twig' %}

{% block title %}New Job{% endblock %}

{% block body %}
    <h1>Create new Job</h1>

    {{ include('job/_form.html.twig') }}
    {% if is_granted('ROLE_ADMIN')%}
    <a class="btn btn-danger" href="{{ path('job_index') }}">Admin Job Menu</a>
    {% endif %}
{% endblock %}
